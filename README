РАЗВЁРТЫВАНИЕ ZABBIX В ASTRA LINUX 1.7, 1.8
        Установка:
    подключить установочный диск
    sudo apt update
    cd <КАТАЛОГ C zabastra-mrd.deb>
    sudo apt install ./zabastra-mrd.deb -y

        В процессе установки указать ( либо согласиться со значением по умолчанию)
            -часовой пояс (свой указываем с экранированием / как в примере)
            -пароль администратора
    

